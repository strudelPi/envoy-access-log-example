events {}
http {
    server {
        listen 8123;
        server_name localhost;

        location / {
            return 200 'Welcome to Nginx!';
            add_header Content-Type text/plain;
        }
    }
}
